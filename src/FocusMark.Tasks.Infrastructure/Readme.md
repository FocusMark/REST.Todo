To configure for running you must first install the local tooling required for EF core. Do this with the `dotnet` CLI:

```
dotnet tool restore
```

This will install the tooling defined in the `.config/dotnet-tools.json` manifest file.